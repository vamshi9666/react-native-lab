import React from "react";

import ModalContext from "../cotext/ModalContext";

export default class SharedModal extends React.Component {
  render() {
    const style = {
      postion: "absolute",
      width: width.value,
      height: height.value,
      left: x.value,
      top: y.value
    };
    return <View></View>;
  }
}
